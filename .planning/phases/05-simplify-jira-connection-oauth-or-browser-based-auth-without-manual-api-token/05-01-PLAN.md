---
phase: 05-simplify-jira-connection-oauth-or-browser-based-auth-without-manual-api-token
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/commands/configure.ts
  - src/commands/configure.test.ts
  - src/lib/jira.ts
  - src/lib/jira.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Interactive configure opens browser to Atlassian API token page after collecting URL and email"
    - "When browser cannot open (SSH/headless), URL is printed with manual instructions"
    - "Non-interactive mode (--url --email --token) does NOT open browser"
    - "401 JIRA API errors show clear message directing user to run treeji configure again"
    - "TREEJI_JIRA_TOKEN env var fallback continues to work"
  artifacts:
    - path: "src/commands/configure.ts"
      provides: "Browser-guided token setup flow"
      contains: "id.atlassian.com/manage-profile/security/api-tokens"
    - path: "src/lib/jira.ts"
      provides: "401 detection with re-configure message"
      contains: "treeji configure"
  key_links:
    - from: "src/commands/configure.ts"
      to: "open"
      via: "import open from 'open'"
      pattern: "import open from 'open'"
    - from: "src/lib/jira.ts"
      to: "error message"
      via: "401 detection in withRetry"
      pattern: "401.*treeji configure"
---

<objective>
Improve the `treeji configure` UX by auto-opening the Atlassian API token page in the browser after collecting URL and email, and add 401 error detection with a clear re-configure message.

Purpose: Users no longer need to know or manually navigate to the Atlassian token management URL. On auth failures, they get actionable guidance.
Output: Updated configure.ts with browser-open step, updated jira.ts with 401 detection, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-simplify-jira-connection-oauth-or-browser-based-auth-without-manual-api-token/05-CONTEXT.md
@.planning/phases/05-simplify-jira-connection-oauth-or-browser-based-auth-without-manual-api-token/05-RESEARCH.md

@src/commands/configure.ts
@src/commands/configure.test.ts
@src/lib/jira.ts
@src/lib/jira.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add browser-guided token flow to configure command</name>
  <files>package.json, src/commands/configure.ts, src/commands/configure.test.ts</files>
  <read_first>src/commands/configure.ts, src/commands/configure.test.ts, package.json</read_first>
  <behavior>
    - Test: Interactive flow calls `open` with `https://id.atlassian.com/manage-profile/security/api-tokens` after email prompt and before token prompt
    - Test: When `open()` succeeds, `p.note()` is called with text containing "Browser opened"
    - Test: When `open()` rejects (headless/SSH), `p.note()` is called with the full URL `https://id.atlassian.com/manage-profile/security/api-tokens` for manual copy
    - Test: Non-interactive mode (--url --email --token) does NOT call `open()` at all
    - Test: Existing cancel-signal test still passes (cancel exits cleanly without saving)
  </behavior>
  <action>
1. Run `npm install open` to add the `open` package (v11, ESM-native) as a production dependency (per D-01, D-05).

2. In `src/commands/configure.test.ts`, add mock for `open` at the top alongside existing mocks:
```typescript
const mockOpen = vi.fn().mockResolvedValue(undefined);
vi.mock('open', () => ({ default: (...args: unknown[]) => mockOpen(...args) }));
```
Add `mockOpen.mockClear()` in the `beforeEach` block.

Add these new test cases inside the `'interactive mode (no flags)'` describe block:

- "opens browser to Atlassian token page after email prompt (D-05)": Set up mockText to return URL and email, mockPassword to return token, mockValidateJiraCredentials to return success. Run `program.parseAsync(['configure'], { from: 'user' })`. Assert `mockOpen` was called with `'https://id.atlassian.com/manage-profile/security/api-tokens'`. Assert `mockNote` was called with content containing `'Browser opened'`.

- "falls back to printed URL when browser open fails (D-05)": Set `mockOpen.mockRejectedValueOnce(new Error('spawn failed'))`. Run interactive configure. Assert `mockNote` was called with content containing `'https://id.atlassian.com/manage-profile/security/api-tokens'` and title containing `'Open in your browser'`.

Add a test in the `'non-interactive mode'` describe block:
- "non-interactive mode does not open browser": Run with --url --email --token flags. Assert `mockOpen` was NOT called.

3. In `src/commands/configure.ts`:
- Add `import open from 'open';` at the top.
- Define constant: `const ATLASSIAN_TOKEN_URL = 'https://id.atlassian.com/manage-profile/security/api-tokens';`
- In the interactive `else` branch, AFTER the `emailResult` prompt and its cancel check, BEFORE the `tokenResult` prompt, insert:

```typescript
let browserOpened = false;
try {
  await open(ATLASSIAN_TOKEN_URL);
  browserOpened = true;
} catch {
  browserOpened = false;
}

if (browserOpened) {
  p.note(
    'Opening browser to Atlassian API token page.\nCreate a new token, copy it, then paste it below.',
    'Browser opened'
  );
} else {
  p.note(
    `Open this URL to create an API token:\n${ATLASSIAN_TOKEN_URL}\n\nCopy the token, then paste it below.`,
    'Open in your browser'
  );
}
```

- The `isNonInteractive` branch remains unchanged — no `open()` call there (per D-02, Pitfall 3).

4. Run `npm test -- --run src/commands/configure.test.ts` to verify all tests pass.
  </action>
  <verify>
    <automated>npm test -- --run src/commands/configure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"open"` in `dependencies` (not devDependencies)
    - `src/commands/configure.ts` contains `import open from 'open'`
    - `src/commands/configure.ts` contains `const ATLASSIAN_TOKEN_URL = 'https://id.atlassian.com/manage-profile/security/api-tokens'`
    - `src/commands/configure.ts` contains `await open(ATLASSIAN_TOKEN_URL)` inside a try block
    - `src/commands/configure.ts` contains `p.note(` with `'Browser opened'` string
    - `src/commands/configure.ts` contains `p.note(` with `'Open in your browser'` string
    - `src/commands/configure.test.ts` contains `vi.mock('open'`
    - `src/commands/configure.test.ts` contains test case with string `'opens browser to Atlassian token page'`
    - `src/commands/configure.test.ts` contains test case with string `'falls back to printed URL'`
    - `src/commands/configure.test.ts` contains assertion `expect(mockOpen).not.toHaveBeenCalled()`
    - `npm test -- --run src/commands/configure.test.ts` exits 0
  </acceptance_criteria>
  <done>Interactive configure opens browser after email prompt; headless falls back to printed URL; non-interactive skips browser; all configure tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 401 detection with re-configure message to JIRA client</name>
  <files>src/lib/jira.ts, src/lib/jira.test.ts</files>
  <read_first>src/lib/jira.ts, src/lib/jira.test.ts</read_first>
  <behavior>
    - Test: When a JIRA API call throws an error containing "401", withRetry throws with message containing "treeji configure"
    - Test: When a JIRA API call throws an error containing "Unauthorized", withRetry throws with message containing "treeji configure"
    - Test: 401 errors are NOT retried (thrown immediately, unlike 429 which retries)
    - Test: Existing 429 retry behavior still works (not broken by 401 detection)
  </behavior>
  <action>
1. In `src/lib/jira.test.ts`, add a new `describe('withRetry 401 detection (D-09)')` block. Since `withRetry` is not exported, test through `fetchIssue` which uses it internally.

Add these test cases:

- "401 error includes treeji configure instruction": Set `mockGetIssue.mockRejectedValue(new Error('Request failed with status code 401'))`. Call `fetchIssue('TEST-1')`. Assert it rejects with error message matching `/treeji configure/`.

- "Unauthorized error includes treeji configure instruction": Set `mockGetIssue.mockRejectedValue(new Error('Unauthorized'))`. Call `fetchIssue('TEST-1')`. Assert it rejects with error message matching `/treeji configure/`.

- "401 errors are not retried": Set `mockGetIssue.mockRejectedValue(new Error('401 Unauthorized'))`. Call `fetchIssue('TEST-1')` and catch. Assert `mockGetIssue` was called exactly 1 time (no retries).

- "429 errors still retry (not broken by 401 detection)": Set `mockGetIssue.mockRejectedValueOnce(new Error('429 Too Many Requests')).mockResolvedValueOnce({ fields: { summary: 'Test', status: { name: 'Open' } } })`. Call `fetchIssue('TEST-1')`. Assert it resolves successfully and `mockGetIssue` was called 2 times.

2. In `src/lib/jira.ts`, modify the `withRetry` function. Inside the catch block, BEFORE the existing 429 check, add 401 detection:

```typescript
const is401 = msg.includes('401') || msg.toLowerCase().includes('unauthorized');
if (is401) {
  throw new Error('JIRA authentication failed (401 Unauthorized). Run `treeji configure` again to update your API token.');
}
```

This ensures 401 errors are thrown immediately (no retry), and the error message directs the user to re-configure (per D-09).

3. Run `npm test -- --run src/lib/jira.test.ts` to verify all tests pass.

4. Run full test suite `npm test -- --run` to verify no regressions.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/jira.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/jira.ts` contains `const is401 = msg.includes('401') || msg.toLowerCase().includes('unauthorized')`
    - `src/lib/jira.ts` contains string `treeji configure`
    - `src/lib/jira.ts` contains `if (is401)` BEFORE `if (!is429`
    - `src/lib/jira.test.ts` contains describe block with string `'401 detection'`
    - `src/lib/jira.test.ts` contains test with string `'treeji configure instruction'`
    - `src/lib/jira.test.ts` contains test with string `'401 errors are not retried'`
    - `npm test -- --run src/lib/jira.test.ts` exits 0
    - `npm test -- --run` exits 0 (full suite, minus pre-existing switch.test.ts failures)
  </acceptance_criteria>
  <done>401 JIRA errors throw immediately with actionable message directing user to run treeji configure; 429 retry behavior preserved; all jira tests pass.</done>
</task>

</tasks>

<verification>
1. `npm test -- --run` — full test suite passes (minus pre-existing switch.test.ts failures)
2. `npm run build` — production build succeeds with new `open` dependency
3. Interactive configure flow: after URL + email prompts, browser opens to Atlassian token page
4. Non-interactive `treeji configure --url X --email Y --token Z` works without browser open
</verification>

<success_criteria>
- Browser auto-opens to `https://id.atlassian.com/manage-profile/security/api-tokens` during interactive configure (D-01, D-05)
- Headless/SSH gracefully falls back to printing the URL (D-05)
- Non-interactive mode unchanged — no browser open (Pitfall 3)
- 401 errors produce clear "Run `treeji configure` again" message (D-09)
- TREEJI_JIRA_TOKEN env var fallback continues working (D-08 — no changes to keychain.ts)
- All configure and jira tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-simplify-jira-connection-oauth-or-browser-based-auth-without-manual-api-token/05-01-SUMMARY.md`
</output>
