---
phase: quick-260406-9ea
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/remove.ts
  - src/commands/remove.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Before the picker, blocked worktrees are printed with their reason(s)"
    - "Dirty worktrees show 'uncommitted changes'"
    - "Local-only branch worktrees show 'branch not pushed to remote'"
    - "Worktrees with both issues show both reasons joined by ', '"
    - "Safe-only picker still works as before when no blocked worktrees exist"
    - "If no safe worktrees exist, 'No worktrees can be safely removed' is still shown"
  artifacts:
    - path: src/commands/remove.ts
      provides: "Blocked-worktree listing before interactive picker"
    - path: src/commands/remove.test.ts
      provides: "Tests for blocked-worktree output"
  key_links:
    - from: src/commands/remove.ts
      to: chalk
      via: "import chalk from 'chalk'"
      pattern: "chalk\\.yellow|chalk\\.dim"
---

<objective>
Print blocked worktrees (dirty or branch-not-on-remote) with their reason(s) before the interactive `treeji remove` picker.

Purpose: Users understand why certain worktrees are missing from the picker without confusion.
Output: Modified remove.ts interactive path + new test cases covering the blocked-list output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/commands/remove.ts
@src/commands/remove.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Show blocked worktrees before interactive picker</name>
  <files>src/commands/remove.ts, src/commands/remove.test.ts</files>
  <behavior>
    - Test BLOCKED DIRTY: worktree with dirty status and branch on remote → printed in blocked list with reason "uncommitted changes"; does NOT appear in picker options
    - Test BLOCKED NO REMOTE: worktree with clean status but branch not on remote → printed with "branch not pushed to remote"; does NOT appear in picker
    - Test BLOCKED BOTH: worktree dirty AND branch not on remote → printed with "uncommitted changes, branch not pushed to remote"
    - Test NO BLOCKED: all safe worktrees → no blocked header printed, picker shown as before (existing test remains green)
    - Test ALL BLOCKED NO SAFE: only blocked worktrees → blocked list printed, then "No worktrees can be safely removed" outro (existing test updated)
  </behavior>
  <action>
In `src/commands/remove.ts`, replace the interactive picker block (the `if (!name)` branch, lines 24–101) with a new implementation that:

1. Adds `import chalk from 'chalk';` at the top (after existing imports).

2. In the Promise.all categorisation loop, instead of returning `wt | null`, collect two arrays:
   - `safeWorktrees: WorktreeInfo[]`
   - `blockedWorktrees: Array<{ wt: WorktreeInfo; reasons: string[] }>`

   For each nonMain worktree:
   ```
   const statusOutput = await gitStatusPorcelain(wt.path);
   const isDirty = statusOutput.trim().length > 0;
   const onRemote = await gitBranchExistsOnRemote(wt.branch!);
   const reasons: string[] = [];
   if (isDirty) reasons.push('uncommitted changes');
   if (!onRemote) reasons.push('branch not pushed to remote');
   if (reasons.length === 0) { safeWorktrees.push(wt); }
   else { blockedWorktrees.push({ wt, reasons }); }
   ```

   Use `Promise.all` that returns `{ safe, blocked }` tuples, then reduce into the two arrays.

3. If `blockedWorktrees.length > 0`, print BEFORE the picker (use `console.log`):
   ```
   console.log('Cannot remove:');
   for (const { wt, reasons } of blockedWorktrees) {
     const name = path.basename(wt.path);
     console.log(`  ${chalk.yellow(name)}  ${chalk.dim('— ' + reasons.join(', '))}`);
   }
   console.log('');
   ```

4. The rest of the picker flow is unchanged: if `safeWorktrees.length === 0` → `p.outro('No worktrees can be safely removed')` + `process.exit(0)`, otherwise show `p.select(...)`.

In `src/commands/remove.test.ts`, add a `mockLog` spy to capture `console.log` output (similar to the existing `stderrSpy`), cleared in `beforeEach`, restored in `afterEach`. Add the five new test cases described in the `<behavior>` block above, asserting that `mockLog` was called with strings matching the worktree name and reason(s).

Do NOT modify the named-worktree path (`if (name)` branch) — those tests must stay green.
  </action>
  <verify>
    <automated>cd /Users/jpisa/Development/Claude/mafin-cli && npx vitest run src/commands/remove.test.ts</automated>
  </verify>
  <done>All existing remove tests pass. Five new tests pass: BLOCKED DIRTY, BLOCKED NO REMOTE, BLOCKED BOTH, NO BLOCKED, ALL BLOCKED NO SAFE. `console.log` output contains chalk.yellow worktree name and chalk.dim reason string.</done>
</task>

</tasks>

<verification>
`npx vitest run src/commands/remove.test.ts` — all tests pass (existing + new).
</verification>

<success_criteria>
- Blocked worktrees appear before the picker with correct reason text
- Safe picker flow unchanged
- All remove tests green
</success_criteria>

<output>
After completion, create `.planning/quick/260406-9ea-remove-show-blocked-worktrees/260406-9ea-SUMMARY.md`
</output>
