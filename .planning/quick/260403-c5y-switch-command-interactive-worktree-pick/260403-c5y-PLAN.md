---
phase: quick
plan: 260403-c5y
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/switch.ts
  - src/commands/switch.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "mafcli switch <name> still resolves directly and emits sentinel (no regression)"
    - "mafcli switch (no args) presents interactive worktree list and emits sentinel for selection"
    - "Interactive UI renders to stderr — stdout contains exactly one line: the sentinel"
    - "Cancelling the picker exits with code 1 and writes nothing to stdout"
    - "No-worktrees edge case: exits with code 1 and an informative stderr message"
  artifacts:
    - path: "src/commands/switch.ts"
      provides: "Switch command with optional [name] arg and interactive fallback"
    - path: "src/commands/switch.test.ts"
      provides: "Tests for interactive path, cancel, and no-worktrees edge cases"
  key_links:
    - from: "src/commands/switch.ts"
      to: "@clack/prompts select()"
      via: "output: process.stderr to keep stdout clean"
      pattern: "select\\(.*output.*process\\.stderr"
    - from: "src/commands/switch.ts"
      to: "process.stdout.write"
      via: "sentinel emission — only call after selection confirmed"
      pattern: "__MAFCLI_CD__"
---

<objective>
Extend `mafcli switch` so that calling it without arguments shows an interactive worktree picker (via `@clack/prompts select()`) instead of erroring. The existing `mafcli switch <name>` shortcut is preserved unchanged. The selected path is emitted via the existing `__MAFCLI_CD__:path` sentinel protocol.

Purpose: Lets the user run `mafcd` alone (no argument) and pick from all available worktrees interactively — same UX pattern as `mafcli pick` for JIRA issues.
Output: Updated switch.ts and switch.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Existing decision (non-negotiable): "switch command: only stdout output is __MAFCLI_CD__:path sentinel — zero console.log or @clack/prompts in switch.ts"
This means all @clack/prompts UI output MUST go to process.stderr via the `output` option.

Key @clack/prompts API facts (confirmed from node_modules):
- `select(opts)` accepts `output?: Writable` (from CommonOptions) — pass `process.stderr`
- `intro(title, opts)`, `outro(msg, opts)`, `cancel(msg, opts)` also accept `{ output: process.stderr }`
- `isCancel(value)` detects Ctrl+C / cancel symbol
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend switch command with interactive picker fallback</name>
  <files>src/commands/switch.ts</files>
  <behavior>
    - When called with name arg: same behavior as today (find worktree, emit sentinel or exit 1)
    - When called without name arg AND no worktrees available: stderr message "no worktrees found", exit 1
    - When called without name arg AND worktrees exist: show select() prompt, on selection emit sentinel
    - When user cancels the prompt (Ctrl+C → isCancel): stderr message "cancelled", exit 1, no stdout
    - All @clack/prompts calls pass `output: process.stderr` — stdout is ONLY the sentinel line
  </behavior>
  <action>
Change Commander argument from `switch <name>` (required) to `switch [name]` (optional).

In the action handler:
1. If `name` is provided: run existing lookup logic unchanged (no behavioral change).
2. If `name` is undefined:
   - Call `gitWorktreeList()` and `parseWorktreeList()` as before.
   - If `worktrees.length === 0`: `process.stderr.write('mafcli: no worktrees found\n')` → `process.exit(1)`.
   - Build select options: map each WorktreeInfo to `{ value: wt.path, label: path.basename(wt.path), hint: wt.branch ?? 'detached HEAD' }`. Include all worktrees (including main — user may want to switch back).
   - Call `await select({ message: 'Switch to worktree', options, output: process.stderr })`.
   - If `isCancel(result)`: `process.stderr.write('mafcli: cancelled\n')` → `process.exit(1)`.
   - Otherwise emit: `process.stdout.write(\`__MAFCLI_CD__:\${result}\n\`)`.

Imports to add: `select, isCancel` from `@clack/prompts`.

Do NOT use `intro()` or `outro()` — keep the interactive path minimal to avoid extra stderr noise that clutters the terminal before mafcd takes over.
  </action>
  <verify>
    <automated>cd /Users/jpisa/Development/Claude/mafin-cli && npx vitest run src/commands/switch.test.ts 2>&1</automated>
  </verify>
  <done>All existing switch tests pass. New tests for interactive path, cancel, and no-worktrees all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for interactive picker paths</name>
  <files>src/commands/switch.test.ts</files>
  <action>
Add a second `describe` block: `'switch command — interactive mode (no name arg)'`.

Mock `@clack/prompts` at the top of the file alongside the existing git mock:

```typescript
const mockSelect = vi.fn();
const mockIsCancel = vi.fn();

vi.mock('@clack/prompts', () => ({
  select: (...args: unknown[]) => mockSelect(...args),
  isCancel: (...args: unknown[]) => mockIsCancel(...args),
}));
```

Add `mockSelect.mockClear()` and `mockIsCancel.mockClear()` to `beforeEach`.

Tests to add:

1. **INTERACTIVE PICK: emits sentinel for selected worktree**
   - `mockParseWorktreeList` returns 2 worktrees
   - `mockSelect` resolves to `'/home/user/feat'`
   - `mockIsCancel` returns `false`
   - Parse `['switch']` (no name)
   - Expect `stdoutSpy` called once with `'__MAFCLI_CD__:/home/user/feat\n'`

2. **INTERACTIVE PICK: select() called with output:process.stderr**
   - Same setup
   - Expect `mockSelect` called with object containing `output: process.stderr`
   - This guards the invariant that UI never pollutes stdout

3. **INTERACTIVE CANCEL: exits 1, nothing written to stdout**
   - `mockSelect` resolves to a symbol (use `Symbol()` as cancel sentinel)
   - `mockIsCancel` returns `true`
   - Expect rejects with exit 1
   - Expect `stdoutSpy` not called

4. **NO WORKTREES: exits 1 with stderr message**
   - `mockParseWorktreeList` returns `[]`
   - Parse `['switch']`
   - Expect rejects with exit 1
   - Expect `stderrSpy` called (message contains 'no worktrees')
   - Expect `stdoutSpy` not called
   - Expect `mockSelect` not called (no prompt when list is empty)

5. **INTERACTIVE PICK: stdout has exactly one line**
   - Same as test 1 but assert `stdoutSpy` called exactly once and output ends with `\n` and contains no other newlines before the trailing one

Note: The `vi.resetModules()` in `beforeEach` means each test re-imports `switch.js`. Keep mock setup in the module-level scope (outside describe) so mocks survive the reset. If `vi.resetModules()` causes issues with the clack mock, move the clack mock inside the new describe's `beforeEach` using `vi.doMock`.
  </action>
  <verify>
    <automated>cd /Users/jpisa/Development/Claude/mafin-cli && npx vitest run src/commands/switch.test.ts 2>&1</automated>
  </verify>
  <done>All 5 existing tests pass. All 5 new interactive-mode tests pass. Total: 10 tests green.</done>
</task>

</tasks>

<verification>
Run full test suite to catch any regressions:

```
cd /Users/jpisa/Development/Claude/mafin-cli && npx vitest run 2>&1
```

TypeScript compile check:

```
cd /Users/jpisa/Development/Claude/mafin-cli && npx tsc --noEmit 2>&1
```
</verification>

<success_criteria>
- `mafcli switch my-feat` still resolves and emits `__MAFCLI_CD__:/path/my-feat` (no regression)
- `mafcli switch` (no args) triggers select() with `output: process.stderr`
- On selection: exactly one stdout line `__MAFCLI_CD__:/selected/path`
- On cancel (Ctrl+C): exit 1, zero stdout writes
- On empty worktree list: exit 1, informative stderr, zero stdout writes
- All 10 tests pass, full vitest suite green, tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/quick/260403-c5y-switch-command-interactive-worktree-pick/260403-c5y-SUMMARY.md`
</output>
