---
phase: quick-260406-97m
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/git.ts
  - src/lib/git.test.ts
  - src/commands/remove.ts
  - src/commands/remove.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "gitBranchMergedInto returns true when git merge-base --is-ancestor exits 0"
    - "gitBranchMergedInto returns false when exit code is 1 (not merged)"
    - "gitBranchMergedInto returns false when execa throws"
    - "Interactive picker deletes immediately when branch is merged into main"
    - "Interactive picker shows p.confirm when branch is NOT merged into main"
    - "Interactive picker aborts with exit 1 when user declines unmerged-branch confirm"
  artifacts:
    - path: src/lib/git.ts
      provides: "gitBranchMergedInto export"
      contains: "git merge-base --is-ancestor"
    - path: src/commands/remove.ts
      provides: "merge-check in interactive picker flow"
      contains: "gitBranchMergedInto"
  key_links:
    - from: src/commands/remove.ts
      to: src/lib/git.ts
      via: "gitBranchMergedInto import"
      pattern: "gitBranchMergedInto"
---

<objective>
Add merge-awareness to the interactive `remove` picker so that unmerged remote branches prompt for confirmation before deletion.

Purpose: Prevent accidental loss of unmerged work when a branch is on remote but not yet merged into main.
Output: `gitBranchMergedInto` git helper + updated picker flow + tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/git.ts
@src/lib/git.test.ts
@src/commands/remove.ts
@src/commands/remove.test.ts

<interfaces>
<!-- Existing git helpers the executor must NOT break -->

From src/lib/git.ts (relevant exports):
```typescript
export async function gitDeleteBranch(branch: string, force = false): Promise<void>
export async function gitBranchExistsOnRemote(branch: string): Promise<boolean>
```

From src/commands/remove.ts (interactive picker block, lines 27-86):
- Filters to safeWorktrees (clean + on remote)
- After p.select(), currently calls gitWorktreeRemove + gitDeleteBranch(branch, false) + gitWorktreePrune
- THIS IS THE BLOCK TO UPDATE — add merge check between p.select() result and deletion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gitBranchMergedInto to git.ts + tests</name>
  <files>src/lib/git.ts, src/lib/git.test.ts</files>
  <behavior>
    - Test: execa resolves with exitCode 0 → returns true (merged)
    - Test: execa rejects with exitCode 1 → returns false (not merged)
    - Test: execa rejects with any other error → returns false (safe default)
  </behavior>
  <action>
RED first — add describe('gitBranchMergedInto') block to src/lib/git.test.ts:
- Add import of gitBranchMergedInto to the import line at top of file (alongside existing imports)
- Three tests using mockExeca, matching the behavior block above
- For exitCode 0: mockExeca.mockResolvedValue({ exitCode: 0 } as never)
- For exitCode 1: mockExeca.mockRejectedValue(Object.assign(new Error('not ancestor'), { exitCode: 1 }))
- For other error: mockExeca.mockRejectedValue(new Error('some git error'))
- Run tests — they MUST fail (function not yet exported)

GREEN — add to src/lib/git.ts after gitBranchExistsOnRemote:
```typescript
export async function gitBranchMergedInto(branch: string, target: string): Promise<boolean> {
  try {
    await execa('git', ['merge-base', '--is-ancestor', branch, target]);
    return true;
  } catch {
    return false;
  }
}
```
- Run tests — all three gitBranchMergedInto tests MUST pass
  </action>
  <verify>
    <automated>npx vitest run src/lib/git.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All git.test.ts tests pass including three new gitBranchMergedInto tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update interactive picker in remove.ts + tests</name>
  <files>src/commands/remove.ts, src/commands/remove.test.ts</files>
  <behavior>
    - Test MERGED: gitBranchMergedInto returns true → gitDeleteBranch called with (branch, true), no p.confirm shown
    - Test UNMERGED ACCEPT: gitBranchMergedInto returns false → p.confirm shown, user accepts → gitDeleteBranch called with (branch, true)
    - Test UNMERGED REJECT: gitBranchMergedInto returns false → p.confirm shown, user declines → exit 1, gitWorktreeRemove NOT called
  </behavior>
  <action>
RED first — add tests to src/commands/remove.test.ts:

1. Add mockGitBranchMergedInto to the mock declarations block (alongside mockGitBranchExistsOnRemote at top)
2. Add 'gitBranchMergedInto' to the vi.mock('../lib/git.js', ...) factory object
3. Add mockGitBranchMergedInto.mockClear() to beforeEach
4. Add three new tests inside describe('remove command'):

```
it('INTERACTIVE MERGED: no confirm shown, gitDeleteBranch called with force=true', ...)
it('INTERACTIVE UNMERGED ACCEPT: confirm shown, user accepts → deleted with force=true', ...)
it('INTERACTIVE UNMERGED REJECT: confirm shown, user rejects → exit 1, gitWorktreeRemove not called', ...)
```

Setup for all three: single safe worktree (clean + on remote), mockSelect resolves to it, mockIsCancel returns false.
- MERGED test: mockGitBranchMergedInto.mockResolvedValue(true); expect mockConfirm NOT called; expect mockGitDeleteBranch called with (branch, true)
- UNMERGED ACCEPT: mockGitBranchMergedInto.mockResolvedValue(false); mockConfirm.mockResolvedValue(true); mockIsCancel returns false for both select and confirm; expect mockGitDeleteBranch called with (branch, true)
- UNMERGED REJECT: mockGitBranchMergedInto.mockResolvedValue(false); mockConfirm.mockResolvedValue(false); rejects with 'exit 1'; mockGitWorktreeRemove NOT called

Run tests — the three new tests MUST fail.

GREEN — update src/commands/remove.ts:

1. Add gitBranchMergedInto to the import from '../lib/git.js'
2. In the interactive picker block, after `if (p.isCancel(selected)) { ... }` and before the spinner, insert merge check:

```typescript
const isMerged = await gitBranchMergedInto(pickedWorktree.branch!, 'main');
if (!isMerged) {
  const confirmDelete = await p.confirm({
    message: `Branch '${pickedWorktree.branch}' is not merged into main. Delete anyway?`,
    initialValue: false,
  });
  if (p.isCancel(confirmDelete) || !confirmDelete) {
    p.cancel('Aborted.');
    process.exit(1);
  }
}
```

3. Change the existing gitDeleteBranch call in the interactive block from `gitDeleteBranch(pickedWorktree.branch, false)` to `gitDeleteBranch(pickedWorktree.branch, true)` — force=true is safe because branch is on remote AND either merged or user confirmed.

Run tests — all tests in remove.test.ts MUST pass including the existing ones (INTERACTIVE SELECT + DELETE test will now need force=true — update its assertion from false to true).

NOTE: The existing 'INTERACTIVE SELECT + DELETE' test asserts `mockGitDeleteBranch.toHaveBeenCalledWith('feature/feat-a', false)`. After this change it will assert `true`. Update that assertion in the test to match the new behavior. Also ensure mockGitBranchMergedInto is set to resolve(true) in that test so confirm is skipped.
  </action>
  <verify>
    <automated>npx vitest run src/commands/remove.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All remove.test.ts tests pass including three new merge-check tests. Full suite: npx vitest run passes green.</done>
</task>

</tasks>

<verification>
npx vitest run 2>&1 | tail -30
</verification>

<success_criteria>
- gitBranchMergedInto exported from src/lib/git.ts, uses git merge-base --is-ancestor
- Interactive picker in remove.ts imports and calls gitBranchMergedInto after p.select
- Merged branches: deleted immediately with force=true, no confirm
- Unmerged branches: p.confirm shown with initialValue:false; accept → delete with force=true; reject → exit 1
- All vitest tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/quick/260406-97m-remove-picker-merge-check/260406-97m-SUMMARY.md`
</output>
